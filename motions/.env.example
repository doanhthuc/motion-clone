# =========================================================
# Motions — environment variables
# Copy: cp .env.example .env, rồi điền giá trị thật.
# =========================================================

# =========================================================
# motion-backend (job motion KHÔNG còn qua Supabase — gọi qua proxy /api/motion/*)
# Server-side only → key KHÔNG lộ ra browser.
# =========================================================
NUXT_MOTION_API_URL=https://motion-server.datools.info
NUXT_MOTION_API_KEY=

NUXT_PUBLIC_MOTION_BACKEND_URL=https://motion-server.datools.info
